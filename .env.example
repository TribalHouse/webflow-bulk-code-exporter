# Required only for inventory generation through the documented Webflow Data API.
# Create a Webflow API token with read access to the sites you are allowed to export.
WEBFLOW_API_TOKEN=

# Optional pacing overrides.
# WEBFLOW_DOMAIN_DELAY_MS=750
# WEBFLOW_TAB_WORKERS=1
# WEBFLOW_MIN_DELAY_MS=45000
# WEBFLOW_MAX_DELAY_MS=120000
# WEBFLOW_EXPORT_TIMEOUT_MS=600000
# WEBFLOW_EXPORT_RETRIES=2
